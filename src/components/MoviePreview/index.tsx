import { MoviePreviewDetails } from './MoviePreviewDetails'
import { MoviePreviewRoot } from './MoviePreviewRoot'
import { MoviePreviewTags } from './MoviePreviewTags'
import { MoviePreviewTitle } from './MoviePreviewTitle'

export const MoviePreview = {
  Root: MoviePreviewRoot,
  Title: MoviePreviewTitle,
  Details: MoviePreviewDetails,
  Tags: MoviePreviewTags,
}
